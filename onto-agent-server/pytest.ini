[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto

# 标记
markers =
    integration: 集成测试（需要运行数据库和 Jena）
    unit: 单元测试（不需要外部依赖）

# 运行选项
addopts = -v --tb=short

# 日志
log_cli = true
log_cli_level = INFO
